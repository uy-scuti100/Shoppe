import Search from "@/components/(shared)/Search";
import React from "react";

const page = () => {
   return (
      <main className="mt-8 custom-padding">
         <Search />
         <div>help</div>
      </main>
   );
};

export default page;
